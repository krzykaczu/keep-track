import { colors } from './colors';

export const rangeStatus = {
  alert: `linear-gradient(-170deg, ${colors.alert.gradientStart} 0%, ${
    colors.alert.gradientEnd
  } 100%)`,
  warning: `linear-gradient(-170deg, ${colors.warning.gradientStart} 0%, ${
    colors.warning.gradientEnd
  } 100%)`,
  normal: `linear-gradient(-170deg, ${colors.normal.gradientStart} 0%, ${
    colors.normal.gradientEnd
  } 100%)`,
};
